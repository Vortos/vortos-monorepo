<?php

declare(strict_types=1);

namespace {{Namespace}}\Infrastructure\Email;

use Psr\Log\LoggerInterface;
use Vortos\Ses\Contract\BounceHandlerInterface;
use Vortos\Ses\Webhook\BounceNotification;

final class {{ClassName}}BounceHandler implements BounceHandlerInterface
{
    public function __construct(private readonly LoggerInterface $logger)
    {}

    public function handle(BounceNotification $notification): void
    {
        // TODO: handle the bounce
        // $notification->recipient()->address() — the bounced address
        // $notification->isHardBounce()         — true for Permanent bounces
        // $notification->bounceType()            — BounceType enum
        // $notification->diagnosticCode()        — SMTP diagnostic message
    }
}
